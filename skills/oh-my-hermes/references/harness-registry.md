# OMH Harness Registry

Harnesses shape response quality and evidence gates. They are not proof that a separate runtime role exists.

Legend: Tier `quality-tier`; Ladder: evidence steps; Actions: wrapper actions; Privacy `metadata_only`.

## Representative Harnesses

- `coding-handling`: Route implementation requests through scoped context, edit discipline, tests, review, and evidence. Tier `handoff-gated`. L: `coding_delegation_prepared` -> `executor_dispatch_observed` -> `executor_result_observed` -> `verification_recorded` -> `+1 more`. A: `accept_plan`, `show_prompt_handoff`, `copy_prompt_handoff`, `show_runtime_handoff`, `+12 more`. P `metadata_only`.
- `goal-execution`: Keep long-running work tied to explicit goals, checkpoints, and durable evidence. Tier `checkpoint-gated`. L: `goal_created` -> `story_started` -> `checkpoint_recorded` -> `quality_gate_recorded` -> `+1 more`. A: `continue_goal`, `show_status`, `record_checkpoint`, `record_blocker`, `+1 more`. P `metadata_only`.
- `planning`: Turn clarified requirements into an execution-ready plan with tradeoffs and tests. Tier `acceptance-gated`. L: `request_clarified` -> `plan_drafted` -> `option_tradeoffs_recorded` -> `test_strategy_recorded` -> `+2 more`. A: `accept_plan`, `revise_plan`, `cancel`, `prepare_handoff`. P `metadata_only`.
- `research`: Gather current or source-backed evidence before planning or coding handoff. Tier `source-gated`. L: `research_question_scoped` -> `source_boundaries_recorded` -> `primary_sources_checked` -> `source_diversity_checked` -> `+3 more`. A: `show_sources`, `ask_followup`, `record_source`, `prepare_plan`. P `metadata_only`.
- `business-research`: Prepare source-backed business research briefs with evidence and inference boundaries. Tier `source-gated`. L: `business_question_scoped` -> `source_boundary_recorded` -> `source_quality_recorded` -> `source_evidence_recorded` -> `+2 more`. A: `show_sources`, `ask_followup`, `prepare_strategy_brief`, `show_status`. P `metadata_only`.
- `strategy-synthesis`: Turn goals and evidence into strategy options, tradeoffs, and decision-ready notes. Tier `decision-gated`. L: `decision_scope_recorded` -> `options_recorded` -> `tradeoffs_recorded` -> `recommendation_recorded` -> `+1 more`. A: `show_brief`, `revise_brief`, `record_decision`, `show_status`. P `metadata_only`.
- `meeting-facilitation`: Prepare agendas, discussion prompts, decisions, and record templates. Tier `facilitation-gated`. L: `meeting_goal_scoped` -> `agenda_recorded` -> `discussion_prompts_recorded` -> `decisions_needed_recorded` -> `+1 more`. A: `show_agenda`, `revise_brief`, `record_decision`, `show_status`. P `metadata_only`.
- `customer-insight-triage`: Cluster customer feedback and choose the next workflow without defaulting to coding. Tier `triage-gated`. L: `feedback_source_scoped` -> `clusters_recorded` -> `severity_opportunity_recorded` -> `next_workflow_recommended`. A: `show_triage`, `ask_followup`, `prepare_plan`, `show_status`. P `metadata_only`.
- `ops-review`: Summarize observed operating status, risks, blockers, priorities, and follow-up actions. Tier `status-gated`. L: `review_scope_recorded` -> `status_evidence_recorded` -> `risks_blockers_recorded` -> `priorities_recorded` -> `+1 more`. A: `show_status`, `record_blocker`, `record_checkpoint`, `prepare_plan`. P `metadata_only`.
- `operating-rhythm`: Maintain meeting, scrum, sprint, retro, decision, and follow-up history with prepared-vs-observed boundaries. Tier `operations-gated`. L: `operation_rhythm_scoped` -> `record_structure_prepared` -> `decisions_actions_recorded` -> `status_boundary_recorded`. A: `show_record`, `record_decision`, `record_action`, `export_markdown`, `+1 more`. P `metadata_only`.
- `report-package`: Package supplied inputs into reports, executive briefs, and PPT-ready Markdown/JSON outlines. Tier `report-gated`. L: `report_scope_recorded` -> `inputs_organized` -> `package_outline_prepared` -> `approval_boundary_recorded`. A: `show_report`, `export_markdown`, `export_json`, `record_approval`, `+1 more`. P `metadata_only`.
- `materials-package`: Plan, hand off, and verify material-processing work across decks, PDFs, spreadsheets, documents, HWP, Markdown, and binary exports. Tier `material-gated`. L: `material_scope_recorded` -> `source_inputs_organized` -> `format_qa_ladder_prepared` -> `generation_handoff_prepared_if_needed` -> `+1 more`. A: `show_material_plan`, `choose_target_format`, `prepare_generation_handoff`, `record_export`, `+3 more`. P `metadata_only`.
- `img-summary`: Prepare source-specific, premium domain-aware, and poster-archetype-aware visual prompt cards for meetings, reports, PRs, issue feedback, research briefings, and release announcements without claiming image generation. Tier `visual-card-gated`. L: `source_kind_selected` -> `visual_format_selected` -> `poster_archetype_selected` -> `card_copy_prepared` -> `visual_qa_observed_when_available` -> `+4 more`. A: `show_visual_prompt_card`, `copy_visual_prompt`, `revise_visual_card`, `change_visual_language`, `record_visual_qa`, `+6 more`. P `metadata_only`.
- `design-quality-gate`: Prepare superior visual quality gates for web, deck, PDF, poster, and publishing surfaces. Tier `design-pro-gated`. L: `design_quality_scope_recorded` -> `reference_packet_selected` -> `comparative_quality_rubric_prepared` -> `content_hierarchy_reviewed` -> `visual_qa_observed_when_available` -> `+4 more`. A: `prepare_design_quality_gate`, `show_design_quality_gate`, `record_design_reference`, `record_content_qa`, `record_visual_qa`, `prepare_frontend_handoff`, `+5 more`. P `metadata_only`.
- `frontend`: Prepare design-system-driven web UI creation, redesign, polish, accessibility, performance, and visual-QA handoffs. Tier `frontend-design-gated`. L: `frontend_scope_recorded` -> `initial_generation_contract_prepared_when_greenfield` -> `reference_packet_selected_when_available` -> `design_system_contract_prepared` -> `visual_qa_observed_when_available` -> `+5 more`. A: `prepare_frontend_handoff`, `show_frontend_handoff`, `record_design_reference`, `record_surface_quality_matrix`, `prepare_visual_qa`, `record_visual_qa`, `+4 more`. P `metadata_only`.
- `visual-qa`: Prepare observed-only rendered visual QA gates for web, frontend, image, document, and TUI surfaces. Tier `visual-qa-gated`. L: `visual_qa_scope_recorded` -> `viewport_state_capture_matrix_prepared` -> `freshness_rule_recorded` -> `render_capture_manifest_observed` -> `visual_qa_verdict_recorded` -> `+9 more`. A: `prepare_visual_qa`, `show_visual_qa`, `record_render_capture`, `record_browser_capture`, `record_visual_qa`, `record_visual_qa_verdict`, `+5 more`. P `metadata_only`.
- `workspace-audit`: Map repository, skill, prompt, plugin, MCP/tool, hook, config, docs, and runtime surfaces before OMH strengthening. Tier `workspace-audit-gated`. L: `audit_scope_recorded` -> `surface_inventory_observed_when_available` -> `config_security_findings_redacted` -> `capability_gap_matrix_prepared` -> `+1 more`. A: `prepare_workspace_audit`, `show_workspace_audit`, `record_workspace_inventory`, `record_config_security_findings`, `+3 more`. P `metadata_only`.
- `production-audit`: Evaluate release, deploy, security, observability, rollback, docs, and support readiness before launch. Tier `production-readiness-gated`. L: `production_scope_recorded` -> `readiness_matrix_prepared` -> `release_gate_verdict_recorded` -> `rollback_monitoring_plan_prepared` -> `+1 more`. A: `prepare_production_audit`, `show_production_audit`, `record_release_gate`, `record_production_signal`, `+3 more`. P `metadata_only`.
- `verification-gate`: Define and record build, lint, typecheck, test, security, generated-output, review, CI, and DCO evidence before completion or merge. Tier `verification-gated`. L: `verification_scope_recorded` -> `verification_matrix_prepared` -> `check_results_observed_when_available` -> `stale_or_missing_checks_recorded` -> `+1 more`. A: `prepare_verification_gate`, `show_verification_gate`, `record_build_check`, `record_lint_check`, `+5 more`. P `metadata_only`.
- `agent-evaluation`: Compare executor or agent choices on reproducible tasks using quality, cost, time, tool, and evidence metrics. Tier `agent-eval-gated`. L: `agent_eval_scope_recorded` -> `task_benchmark_set_prepared` -> `run_results_observed_when_available` -> `scorecard_prepared` -> `+1 more`. A: `prepare_agent_evaluation`, `show_agent_evaluation`, `record_eval_task`, `record_eval_run`, `+4 more`. P `metadata_only`.
- `rules-distill`: Extract repeated principles from skills, prompts, traces, reviews, and failures into reviewed rule candidates. Tier `rules-distillation-gated`. L: `rules_scope_recorded` -> `source_corpus_reviewed` -> `principle_candidates_prepared` -> `duplication_conflict_report_prepared` -> `+2 more`. A: `prepare_rules_distillation`, `show_rules_distillation`, `record_rule_candidate`, `approve_rule_candidate`, `+3 more`. P `metadata_only`.
- `codebase-onboarding`: Create a repo map, reading path, glossary, risk map, and first-task runway for unfamiliar codebases. Tier `onboarding-gated`. L: `onboarding_scope_recorded` -> `repo_map_observed_when_available` -> `reading_path_prepared` -> `domain_glossary_prepared` -> `first_task_runway_prepared` -> `+1 more`. A: `prepare_codebase_onboarding`, `show_codebase_onboarding`, `record_repo_map`, `record_reading_path`, `record_first_task_runway`, `+3 more`. P `metadata_only`.
- `codegraph-refresh`: Refresh local code intelligence, summarize repo structure, and prepare task-scoped codegraph handoff context. Tier `codegraph-gated`. L: `codegraph_scope_recorded` -> `codegraph_command_plan_prepared` -> `staleness_scope_report_prepared` -> `codegraph_handoff_prepared_when_task_scoped` -> `codegraph_handoff_observed_when_available` -> `+2 more`. A: `prepare_codegraph_refresh`, `show_codegraph_refresh`, `record_codegraph_build`, `record_codegraph_summary`, `record_codegraph_handoff`, `+1 more`. P `metadata_only`.
- `context-budget-review`: Plan compact context, token/cost budgets, summarization checkpoints, and overflow recovery for long agent work. Tier `context-budget-gated`. L: `context_budget_scope_recorded` -> `must_keep_context_pack_prepared` -> `checkpoint_plan_prepared` -> `budget_risk_register_prepared` -> `overflow_recovery_route_prepared` -> `+1 more`. A: `prepare_context_budget_review`, `show_context_budget_review`, `record_must_keep_context`, `record_checkpoint_plan`, `+3 more`. P `metadata_only`.
- `security-safety-review`: Review prompt, tool, secret, dependency, network, and destructive-action risks before agent or code execution. Tier `security-safety-gated`. L: `security_safety_scope_recorded` -> `threat_surface_map_prepared` -> `permission_secret_risk_matrix_prepared` -> `prompt_injection_review_prepared` -> `safe_action_policy_recorded` -> `remediation_handoff_prepared_when_needed`. A: `prepare_security_safety_review`, `show_security_safety_review`, `record_threat_surface`, `record_permission_secret_risk`, `record_safe_action_policy`, `prepare_remediation_handoff`, `+2 more`. P `metadata_only`.
- `source-finder`: Prepare typed source candidates, acquisition states, observation provenance, and downstream workflow choices without doing network acquisition. Tier `source-acquisition-gated`. L: `source_scope_named` -> `source_kind_selected` -> `candidate_set_prepared` -> `acquisition_status_prepared` -> `+2 more`. A: `prepare_source_finder_plan`, `show_source_candidates`, `record_source_candidate`, `record_source_link_observed`, `+8 more`. P `metadata_only`.
- `paper-learning`: Explain supplied papers or paper PDFs at a chosen level with full section coverage, source-state evidence, and observed-only validation boundaries. Tier `paper-learning-gated`. L: `paper_source_scoped` -> `explanation_level_selected` -> `extraction_state_recorded` -> `coverage_ledger_prepared` -> `+2 more`. A: `choose_explanation_level`, `show_paper_source_requirements`, `record_paper_metadata`, `record_paper_excerpt_observed`, `+7 more`. P `metadata_only`.
- `scheduled-ops-blueprint`: Prepare recurring Hermes operations as schedule/delivery/silence blueprints without claiming runtime execution. Tier `ops-blueprint-gated`. L: `blueprint_scope_recorded` -> `schedule_policy_prepared` -> `delivery_policy_prepared` -> `silence_policy_prepared` -> `+2 more`. A: `show_blueprint`, `revise_schedule`, `confirm_delivery_policy`, `prepare_host_schedule`, `+2 more`. P `metadata_only`.
- `research-department`: Prepare Scout, Analyst, and Briefer research operations with source inbox and briefing status boundaries. Tier `research-ops-gated`. L: `research_plan_scope_recorded` -> `source_inbox_prepared` -> `briefing_status_prepared` -> `tooling_readiness_prepared` -> `+1 more`. A: `show_research_department_plan`, `revise_research_sources`, `confirm_cadence_delivery_tooling`, `record_source_observation`, `+1 more`. P `metadata_only`.
- `reliability-review`: Review incidents, SLOs, error budgets, and remediation follow-ups with strict observed evidence boundaries. Tier `reliability-gated`. L: `reliability_scope_recorded` -> `evidence_boundary_recorded` -> `review_prepared_or_observed` -> `remediation_boundary_recorded`. A: `show_evidence`, `record_gap`, `prepare_handoff`, `record_metric`, `+1 more`. P `metadata_only`.
- `app-delivery-loop`: Run complete app operation loops from idea through decision, handoff, release, deploy, and monitor status. Tier `delivery-gated`. L: `loop_scope_recorded` -> `decision_gate_recorded` -> `plan_or_release_gate_accepted` -> `handoff_prepared_if_needed` -> `+2 more`. A: `show_delivery_loop`, `accept_plan`, `choose_executor`, `prepare_handoff`, `+3 more`. P `metadata_only`.
- `goal-loop`: Run explicit loop invocations through agentic interviewer -> planner -> researcher -> builder -> reviewer -> loop-controller cycles, with task/project/ambition classification, bounded goal shaping, runtime ticks, handoff, feedback, waiting, and resumable status without hidden execution. Tier `loop-gated`. L: `loop_triggered` -> `loopability_assessed` -> `goal_reframed` -> `permission_profile_recorded` -> `+6 more`. A: `assess_loopability`, `convert_to_loop_goal`, `route_direct_task`, `choose_permission_profile`, `+11 more`. P `metadata_only`.
- `deep-interview`: Clarify intent and boundaries one question at a time before planning or execution. Tier `clarity-gated`. L: `ambiguity_identified` -> `blocking_question_asked` -> `answer_recorded` -> `clarified_brief_ready`. A: `answer:clarify`, `cancel`, `rerun_plan`. P `metadata_only`.
- `architect`: Evaluate system boundaries, integration choices, and long-term maintainability. Tier `boundary-gated`. L: `architecture_context_loaded` -> `tradeoffs_recorded` -> `boundary_verdict_recorded`. A: `show_review`, `revise_plan`, `approve_plan`. P `metadata_only`.
- `critic`: Challenge plan consistency, quality criteria, and missing verification. Tier `finding-gated`. L: `review_scope_loaded` -> `findings_recorded` -> `verdict_recorded` -> `residual_risk_recorded`. A: `show_findings`, `request_changes`, `approve_plan`. P `metadata_only`.
- `qa-specialist`: Design adversarial scenarios and verify user-visible behavior before completion. Tier `scenario-gated`. L: `scenario_matrix_defined` -> `checks_run` -> `pass_fail_recorded` -> `fix_followup_recorded_if_needed`. A: `show_status`, `record_check`, `record_blocker`. P `metadata_only`.
- `docs-specialist`: Keep public docs accurate, installable, and aligned with actual behavior. Tier `claim-gated`. L: `claims_scoped` -> `docs_updated` -> `generated_docs_checked` -> `public_claims_verified`. A: `show_docs`, `record_claim_check`, `show_status`. P `metadata_only`.
- `github-event-ops`: Route GitHub PR, issue, CI, and review events into triage, review, labeling, or fix-handoff guidance. Tier `event-gated`. L: `event_received` -> `event_classified` -> `route_card_prepared` -> `mutation_observed_when_available`. A: `show_event_card`, `prepare_review`, `prepare_label`, `prepare_fix_handoff`, `+1 more`. P `metadata_only`.
- `agent-board`: Coordinate multi-Hermes-agent or profile work as board cards with task, handoff, heartbeat, blocker, and completion states. Tier `board-gated`. L: `board_scoped` -> `cards_prepared` -> `heartbeat_recorded_when_available` -> `completion_recorded_when_available`. A: `show_board`, `move_card`, `record_heartbeat`, `record_blocker`, `+1 more`. P `metadata_only`.
- `memory-curation-review`: Review stale, conflicting, duplicate, or risky memory and skill guidance with explicit approve/reject/update actions. Tier `curation-gated`. L: `memory_candidates_scoped` -> `conflicts_ranked` -> `review_actions_prepared` -> `approved_write_observed_when_available`. A: `show_memory_review`, `approve_update`, `reject_update`, `record_memory_write`, `+1 more`. P `metadata_only`.
- `gateway-intent-card`: Normalize gateway session policy for origin, thread, delivery, silent updates, attachments, and status updates. Tier `gateway-gated`. L: `origin_scoped` -> `thread_policy_prepared` -> `delivery_policy_prepared` -> `delivery_observed_when_available`. A: `show_gateway_card`, `confirm_delivery`, `record_delivery`, `record_attachment`, `+1 more`. P `metadata_only`.
- `executor-runtime-readiness`: Compare executor/runtime options by available tools, missing tools, credentials, authority, and handoff mode. Tier `runtime-readiness-gated`. L: `task_runtime_scoped` -> `tool_matrix_prepared` -> `handoff_mode_selected` -> `runtime_dispatch_observed_when_available`. A: `show_runtime_matrix`, `choose_executor`, `prepare_handoff`, `record_dispatch`, `+1 more`. P `metadata_only`.
- `deliverable-package`: Track file deliverables through prepared, generated, QA, approved, attached, and delivered states. Tier `deliverable-gated`. L: `deliverable_scoped` -> `format_plan_prepared` -> `generation_handoff_prepared` -> `file_observed_when_available` -> `+1 more`. A: `show_deliverable_card`, `choose_format`, `prepare_generation_handoff`, `record_file`, `+1 more`. P `metadata_only`.
- `voice-operator`: Convert terse voice/mobile requests into safe clarify, plan, status, handoff, or confirmation actions. Tier `accessibility-gated`. L: `voice_request_received` -> `ambiguity_checked` -> `safe_action_prepared` -> `confirmation_observed_when_required`. A: `ask_clarification`, `confirm_action`, `show_status`, `prepare_handoff`. P `metadata_only`.
- `toolbelt-readiness`: Check required MCP servers, CLIs, APIs, credentials, connectors, and local tools for a workflow. Tier `tool-readiness-gated`. L: `workflow_tools_scoped` -> `tool_requirements_listed` -> `installed_state_recorded_when_available` -> `credential_gaps_recorded`. A: `show_toolbelt`, `open_setup`, `record_tool_check`, `prepare_handoff`, `+1 more`. P `metadata_only`.
- `harness-session-inventory`: Normalize cross-harness session, wrapper, MCP, connector, and worktree state into a drift-aware operator inventory. Tier `session-inventory-gated`. L: `inventory_scope_recorded` -> `session_adapter_matrix_prepared` -> `mcp_inventory_redacted` -> `worktree_lifecycle_snapshot_recorded_when_available` -> `+2 more`. A: `prepare_harness_session_inventory`, `record_harness_session`, `record_mcp_inventory`, `record_worktree_snapshot`, `+1 more`. P `metadata_only`.
- `ops-observability-card`: Report wrapper-safe token, cost, latency, run history, queue, failure-mode, external metric-provider, and service-quality evidence boundaries. Tier `observability-gated`. L: `telemetry_scope_recorded` -> `external_metric_provider_payload_recorded_when_available` -> `local_metrics_summarized` -> `service_quality_gaps_typed` -> `+2 more`. A: `show_observability`, `record_metric`, `record_metric_provider`, `record_failure_mode`, `+1 more`. P `metadata_only`.
- `agent-ops-review`: Prepare a manager-facing quality and throughput review for AI-agent research, coding, review, and status work. Tier `manager-review-gated`. L: `manager_scope_recorded` -> `quality_lanes_prepared` -> `evidence_gaps_named` -> `next_action_selected` -> `+1 more`. A: `show_agent_ops_review`, `choose_ops_lane`, `prepare_research_lane`, `prepare_coding_lane`, `+3 more`. P `metadata_only`.
- `agent-debug`: Prepare an evidence-bounded debugging report for a stuck, looping, drifting, or repeatedly failing agent run. Tier `agent-debug-gated`. L: `failure_state_captured` -> `tool_sequence_recorded_when_available` -> `context_pressure_checked` -> `environment_assumptions_checked` -> `+3 more`. A: `prepare_agent_debug`, `show_agent_debug_report`, `record_agent_failure_capture`, `record_agent_recovery_action`, `+1 more`. P `metadata_only`.
- `failure-signal-audit`: Prepare an evidence-bounded audit of swallowed errors, dangerous fallbacks, propagation gaps, and false-green status claims. Tier `failure-signal-gated`. L: `audit_scope_recorded` -> `source_surfaces_recorded` -> `observed_failure_signals_recorded_when_available` -> `fallback_risk_matrix_prepared` -> `remediation_handoff_prepared_when_needed` -> `+2 more`. A: `prepare_failure_signal_audit`, `show_failure_signal_audit`, `record_failure_signal`, `record_fallback_risk`, `route_to_remediation_or_visual_qa`. P `metadata_only`.
- `instinct-ledger`: Prepare an evidence-bounded ledger of atomic project/global instinct candidates with confidence and promotion review. Tier `instinct-ledger-gated`. L: `instinct_scope_recorded` -> `source_observations_redacted` -> `atomic_instinct_candidates_prepared` -> `confidence_and_domain_scored` -> `+3 more`. A: `prepare_instinct_ledger`, `show_instinct_ledger`, `record_instinct_candidate`, `record_instinct_scope_decision`, `+4 more`. P `metadata_only`.
- `skill-scout`: Prepare a metadata-only scout report for existing skill candidates, external risk review, and adoption options before creating or adapting a skill. Tier `skill-scout-gated`. L: `skill_scout_scope_recorded` -> `search_keywords_prepared` -> `local_candidates_inventory_prepared_when_observed` -> `external_candidates_reviewed_when_observed` -> `+1 more`. A: `prepare_skill_scout`, `show_skill_scout`, `record_skill_candidate`, `record_skill_candidate_risk_review`, `+1 more`. P `metadata_only`.
- `skill-health`: Prepare a metadata-only health dashboard for OMH skills, observed failure signals, pending amendments, and top actions. Tier `skill-health-gated`. L: `skill_health_scope_recorded` -> `catalog_surface_inventory_prepared` -> `failure_signals_clustered_when_observed` -> `pending_amendments_recorded_when_available` -> `+1 more`. A: `prepare_skill_health`, `show_skill_health`, `record_skill_health_signal`, `record_skill_amendment_review`, `+1 more`. P `metadata_only`.
- `workflow-learning`: Route self-improvement signals to memory, skill, wiki, failure-retrospective, automation, or discard review before recording workflow attempts as metadata-only traces, evals, missed-route bundles, candidates, patch proposals, regression cases, audits, indexes, and exports. Tier `learning-gated`. L: `store_destination_classified` -> `store_destination_recorded` -> `store_destination_reviewed` -> `trace_recorded` -> `+7 more`. A: `route_self_improvement_signal`, `review_self_improvement_store_route`, `approve_store_route`, `change_store_route_destination`, `+20 more`. P `metadata_only`.

## Harness Priority

1. Coding requests start with `coding-handling`.
2. Multi-step durable work adds `goal-execution`.
3. Current-source or best-practice questions use the `research` harness and stay in Hermes-side evidence gathering before any coding handoff.
4. Unclear work uses `deep-interview` before `planning`.
5. Risky architecture uses `architect`, then `critic`.
6. User-visible behavior changes add `qa-specialist`.
7. Public commands, examples, or limitations add `docs-specialist`.
